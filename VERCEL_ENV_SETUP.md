# Vercel 环境变量设置指南

## ⚙️ 需要在 Vercel Dashboard 设置的环境变量

由于 `.env.production` 文件不会提交到 Git（安全原因），需要在 Vercel 项目设置中手动添加环境变量。

---

## 📝 操作步骤

### 1. 登录 Vercel Dashboard
访问: https://vercel.com/dashboard

### 2. 进入项目设置
1. 找到 **aireading** 项目
2. 点击项目进入详情页
3. 点击顶部的 **Settings** 标签

### 3. 配置环境变量
1. 在左侧菜单选择 **Environment Variables**
2. 添加以下变量：

#### 变量 1: NEXT_PUBLIC_AUDIO_CDN_URL
```
Key: NEXT_PUBLIC_AUDIO_CDN_URL
Value: https://trading.aireading.com
Environment: Production, Preview (勾选两个)
```

#### 变量 2: NEXT_PUBLIC_SITE_URL
```
Key: NEXT_PUBLIC_SITE_URL
Value: https://aireading.com
Environment: Production, Preview (勾选两个)
```

### 4. 保存并重新部署
1. 点击 **Save** 保存环境变量
2. 返回 **Deployments** 标签
3. 点击最新部署右侧的 **...** 菜单
4. 选择 **Redeploy**
5. 勾选 **Use existing Build Cache**（加快速度）
6. 点击 **Redeploy** 确认

---

## ✅ 验证环境变量

部署完成后，可以通过以下方式验证：

### 方式 1: 检查构建日志
1. 进入 Deployments 页面
2. 点击最新的部署
3. 查看 Build Logs
4. 搜索 `NEXT_PUBLIC_AUDIO_CDN_URL`

### 方式 2: 浏览器控制台
```javascript
// 访问 https://aireading.com
// 打开浏览器控制台，输入：
console.log(process.env.NEXT_PUBLIC_AUDIO_CDN_URL);
// 应该输出: https://trading.aireading.com
```

### 方式 3: 测试音频加载
打开浏览器开发者工具的 Network 标签，访问任意书籍页面，检查音频请求的 URL 是否指向 `trading.aireading.com`。

---

## 🔍 故障排查

### 问题: 环境变量未生效

**原因**: 
- 环境变量只对新的部署生效
- 需要重新部署才能应用

**解决方案**:
```bash
# 方式 1: 通过 Dashboard 重新部署（推荐）
访问 Vercel Dashboard → Deployments → Redeploy

# 方式 2: 通过 Git 触发部署
git commit --allow-empty -m "chore: trigger redeploy for env vars"
git push origin main
```

### 问题: 音频仍然 404

**检查清单**:
1. ✅ 环境变量是否正确设置
2. ✅ trading.aireading.com 服务是否运行
3. ✅ 音频文件是否存在
4. ✅ 浏览器是否有缓存（清除缓存重试）

---

## 📸 设置截图说明

### Environment Variables 页面应该显示：

```
┌────────────────────────────────────────┐
│ Environment Variables                   │
├────────────────────────────────────────┤
│ NEXT_PUBLIC_AUDIO_CDN_URL              │
│ https://trading.aireading.com          │
│ Production, Preview                     │
├────────────────────────────────────────┤
│ NEXT_PUBLIC_SITE_URL                   │
│ https://aireading.com                  │
│ Production, Preview                     │
└────────────────────────────────────────┘
```

---

## ⚠️ 重要提示

1. **Environment 选择**:
   - ✅ **Production**: 必须勾选（用于 aireading.com）
   - ✅ **Preview**: 建议勾选（用于预览部署）
   - ❌ **Development**: 不需要（本地开发用 .env.local）

2. **变量命名**:
   - `NEXT_PUBLIC_` 前缀的变量会暴露到浏览器端
   - 这是 Next.js 的安全机制
   - 不要在这些变量中存储敏感信息

3. **生效时间**:
   - 设置后立即保存
   - 但需要重新部署才会应用
   - 预计生效时间: 2-5 分钟

---

## 🚀 完成后的效果

配置成功后：

### 前端代码
```typescript
import { getAudioUrl } from '@/lib/config';

// 会自动使用 trading.aireading.com
const url = getAudioUrl(29, 'zh', 'short');
console.log(url);
// 输出: https://trading.aireading.com/audio/29/zh_short.mp3
```

### 音频访问
- ✅ https://aireading.com/en/ （页面正常）
- ✅ 音频请求自动转向 trading.aireading.com
- ✅ 无需在 Vercel 上传 308MB 音频文件
- ✅ 部署时间从 5-10 分钟降至 1-2 分钟

---

**设置完成后请告诉我，我会帮你验证！** ✅

---

**创建日期**: 2026-02-09  
**状态**: 等待 Vercel 环境变量设置
